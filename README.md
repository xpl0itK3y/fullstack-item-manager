# Fullstack Item Manager

Fullstack приложение для управления списком из 1 млн элементов с drag-and-drop сортировкой, инфинити-скроллом и фильтрацией. Vue.js + Express.js + Docker.

## ✨ Основной функционал

- Двухпанельный интерфейс (доступные / выбранные элементы)
- Работа с 1 000 000+ элементов
- Drag & Drop сортировка выбранных элементов
- Инфинити-скролл с подгрузкой по 20 элементов
- Фильтрация по ID в реальном времени
- Батчинг запросов с дедупликацией
- Сохранение состояния на сервере
- Готовый Docker контейнер

## 🛠 Технологии

**Frontend:** Vue 3, Composition API, Vite, Axios, vuedraggable  
**Backend:** Express.js, in-memory хранилище, очередь запросов  
**DevOps:** Docker, Docker Compose

## 🚀 Быстрый старт

### Запуск через Docker (рекомендуется)
```bash
git clone https://github.com/xpl0itK3y/fullstack-item-manager.git
cd fullstack-item-manager
docker-compose up -d
```

Приложение доступно:
- Frontend: http://localhost:5173
- Backend: http://localhost:3000

### Локальная разработка
```bash
# Backend
cd backend
npm install
npm run dev

# Frontend (в новом терминале)
cd frontend
npm install
npm run dev
```

## 📁 Структура проекта
```
fullstack-item-manager/
├── backend/
│   ├── src/
│   │   ├── server.js
│   │   ├── routes/
│   │   └── services/
│   ├── Dockerfile
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   └── App.vue
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml
└── README.md
```

## 🔌 API

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/items/available` | Получить доступные элементы |
| GET | `/api/items/selected` | Получить выбранные элементы |
| POST | `/api/items/select` | Добавить в выбранные |
| POST | `/api/items/deselect` | Удалить из выбранных |
| POST | `/api/items/reorder` | Изменить порядок |
| POST | `/api/items/add` | Добавить новый элемент |

**Параметры:** `page`, `limit=20`, `search`

## ⚙️ Особенности реализации

- **Батчинг:** добавление элементов раз в 10 сек, получение/изменение раз в 1 сек
- **Дедупликация:** исключение повторных запросов в очереди
- **In-memory хранилище:** данные в RAM, сбрасываются при перезапуске
- **Общее состояние:** данные доступны всем пользователям

## 🐳 Docker
```bash
docker-compose up -d          # Запуск
docker-compose logs -f        # Логи
docker-compose down           # Остановка
docker-compose up -d --build  # Пересборка
```

## 📊 Производительность

- 1 000 000 элементов = ~100MB RAM
- Загрузка первой страницы < 2 сек
- Батчинг снижает нагрузку на 90%

## 📝 Примечания

- Данные хранятся в памяти, при перезапуске сервера сбрасываются
- Тестовое задание на позицию fullstack разработчика

## 📄 Лицензия

MIT
